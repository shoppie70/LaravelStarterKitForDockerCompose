<input class="form-input form-input__text p-region p-locality p-street-address p-extended-address"
       type="text"
       name="{{ $name }}"
       placeholder="{{ $placeholder ?? '' }}"
       value="{{ $value ?? '' }}"
>
